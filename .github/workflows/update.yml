name: Actualizar Portafolio Automáticamente

on:
  schedule:
    - cron: '0 0 * * *' # Se ejecuta cada día a las 00:00 UTC
  workflow_dispatch: # Te permite ejecutarlo manualmente desde la pestaña "Actions" si quieres

permissions:
  contents: write # Permiso vital para que el robot pueda guardar cambios en tu repo

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Descargar tu código
      uses: actions/checkout@v4

    - name: 🟢 Instalar Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 🚀 Ejecutar script de actualización
      run: node generate.js

    - name: 💾 Guardar cambios (Commit & Push)
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "🤖 Auto-update: database.json actualizado"
        file_pattern: database.json
